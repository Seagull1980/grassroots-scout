*** Begin Patch
*** Update File: src/pages/RegisterPage.tsx
@@
   const [fieldErrors, setFieldErrors] = useState<Record<string, string>>({});
+  const [pendingStep, setPendingStep] = useState<number | null>(null);
@@
-  useEffect(() => {
-    if (currentStep === 2) {
-      const missingFirst = !formData.firstName;
-      const missingLast = !formData.lastName;
-      const missingEmail = !formData.email;
-      if (missingFirst || missingLast || missingEmail) {
-        setFieldErrors(prev => ({
-          ...prev,
-          firstName: missingFirst ? 'First name is required' : prev.firstName,
-          lastName: missingLast ? 'Last name is required' : prev.lastName,
-          email: missingEmail ? 'Email is required' : prev.email
-        }));
-        setCurrentStep(0);
-      }
-    }
-  }, [currentStep, formData.firstName, formData.lastName, formData.email]);
+  useEffect(() => {
+    if (currentStep === 2) {
+      const missingFirst = !formData.firstName;
+      const missingLast = !formData.lastName;
+      const missingEmail = !formData.email;
+      if (missingFirst || missingLast || missingEmail) {
+        setFieldErrors(prev => ({
+          ...prev,
+          firstName: missingFirst ? 'First name is required' : prev.firstName,
+          lastName: missingLast ? 'Last name is required' : prev.lastName,
+          email: missingEmail ? 'Email is required' : prev.email
+        }));
+        // remember intent to reach step 2, then send user to details (0)
+        setPendingStep(2);
+        setCurrentStep(0);
+      }
+    }
+  }, [currentStep, formData.firstName, formData.lastName, formData.email]);
@@
-  const handleNext = () => {
-    setError('');
-    if (validateStep(currentStep)) setCurrentStep(s => s + 1);
-  };
+  const handleNext = () => {
+    setError('');
+    if (!validateStep(currentStep)) return;
+    // If we were redirected to details (0) because a later step required name/email,
+    // jump to the originally intended step after finishing details.
+    if (currentStep === 0 && pendingStep !== null) {
+      setCurrentStep(pendingStep);
+      setPendingStep(null);
+      return;
+    }
+    setCurrentStep(s => s + 1);
+  };
@@
-    try {
-      await register(registrationData);
-      navigate('/email-verification-pending');
-    } catch (error: any) {
+    try {
+      await register(registrationData);
+      setPendingStep(null);
+      navigate('/email-verification-pending');
+    } catch (error: any) {
*** End Patch
