FROM node:18-slim

# Install dependencies
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

# Copy source code
COPY . .

# Build the React app
RUN npm run build

# Expose port
EXPOSE 8080

# Start the application
CMD ["npm", "run", "railway:start"]